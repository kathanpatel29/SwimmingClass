extends layout

block content
  .container
    h2 Admin Dashboard
    .dashboard-links
      a(href='/admin/add-class') Add New Class
      // Add more links or buttons for other CRUD operations as needed
    .dashboard-links
      a(href='/admin/classes') View All Classes
      // Add more links or buttons for other CRUD operations as needed
